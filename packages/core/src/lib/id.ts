export const generatePageId = (): string => `page_${crypto.randomUUID()}`;

export const generateBlockId = (): string => `block_${crypto.randomUUID()}`;

export const generateWorkspaceId = (): string => `ws_${crypto.randomUUID()}`;

export const generateCardId = (): string => `card_${crypto.randomUUID()}`;
